\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[
        node distance=1.2cm and 1.4cm,
        arch_block/.style={rectangle, draw=ImperialBlue, fill=white, text width=2.6cm, align=center, minimum height=1cm, rounded corners, thick, font=\footnotesize\sffamily, drop shadow},
        arch_source/.style={rectangle, draw=CoolGrey, fill=LightBlue!20, text width=2.2cm, align=center, minimum height=0.8cm, font=\footnotesize\sffamily, drop shadow},
        arch_math/.style={diamond, draw=ImperialBlue, fill=LightBlue!10, text width=1.4cm, align=center, inner sep=-2pt, font=\scriptsize\sffamily, drop shadow},
        arrow/.style={-{Stealth[scale=1.0]}, thick, draw=ImperialBlue}
    ]
        % Path 1: JD Weighting
        \node [arch_source] (jd) {\textbf{Job Description}\\Text};
        \node [arch_block, right=of jd] (jd_parse) {Entity Extraction\\(Heuristic NLP)};
        \node [arch_math, right=of jd_parse] (tfidf) {TF-IDF Engine\\$TF \times \log(\frac{N}{df})$};
        \node [arch_block, right=of tfidf] (weights) {Dynamic Priority\\(1.0 - 5.0 Scale)};

        % Path 2: CV Matching
        \node [arch_source, below=2.2cm of jd] (cv) {\textbf{Candidate CV}\\Text};
        \node [arch_block, right=of cv] (cv_parse) {Skill Extraction\\(NLP/Regex)};
        \node [arch_block, right=of cv_parse] (sbert) {SentenceTransformer\\(all-MiniLM-L6-v2)};
        \node [arch_math, right=of sbert] (cosine) {Cosine\\Similarity\\$\cos(\theta) \geq 0.6$};

        % Connections
        \draw [arrow] (jd) -- (jd_parse);
        \draw [arrow] (jd_parse) -- (tfidf);
        \draw [arrow] (tfidf) -- (weights);

        \draw [arrow] (cv) -- (cv_parse);
        \draw [arrow] (cv_parse) -- (sbert);
        \draw [arrow] (sbert) -- (cosine);

        % Interaction
        \draw [arrow, dashed, draw=CoolGrey] (jd_parse.south) -- ++(0,-0.5) -| (sbert.north) 
            node[pos=0.25, below, font=\tiny\itshape, xshift=0.5cm] {Requirement Embeddings};

    \end{tikzpicture}
    \caption{Stage 1: The Intelligent Configuration process for dynamic skill weighting 
    (TF-IDF) and semantic requirement mapping.}
    \label{fig:skill_extraction_pipeline}
\end{figure}
